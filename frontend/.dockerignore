# Git
.git
.gitignore

# Node modules (they are installed within the Docker image)
node_modules

# Build output (it's generated within the Docker image)
dist
build

# IDE / OS specific
.idea
.vscode
*.DS_Store
*.suo
*.user
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite reads public settings from .env during the build. Keep local overrides out.
.env.*
!.env.example
