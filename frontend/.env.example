VITE_API_URL=/api
VITE_API_VERSION=v1
# Vite proxies API and WebSocket requests here during local development.
DEV_BACKEND_URL=http://127.0.0.1:8000
# Optional separate WebSocket origin (ws:// or wss://); otherwise same origin.
VITE_WS_URL=
